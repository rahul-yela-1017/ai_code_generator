import pandas as pd   # We import the library here just for its built-in functions. If you want more functionality, include them explicitly via pip install <module> command later on your IDE's requirement list/toolbar if any python environment setup is required at that point in time too. It would be helpful to keep an eye out of these functionalities as they continuously come up over the years or new Python libraries are introduced by organizations like Facebook, Google etc..